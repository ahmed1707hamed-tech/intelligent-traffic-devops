pipeline {
    agent any

    stages {

        stage('Deploy') {
            steps {
                sh '''
                ssh -i /var/jenkins_home/traffic-key.pem -o StrictHostKeyChecking=no ec2-user@50.19.101.138 "
                cd intelligent-traffic-devops &&
                docker-compose down &&
                docker-compose up -d --build
                "
                '''
            }
        }

    }
}